if TARGET_WARP7

config SYS_BOARD
	default "warp7"

config SYS_CONFIG_NAME
	default "warp7"

config LOADCMD_EXT4
	bool "ext4load"
	help
	  Change load command from fatload to ext4load by default.

endif
